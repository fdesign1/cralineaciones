[build]
  publish = "dist"
  command = "vite build"

[dev]
  command = "npx vite --port 3000 --host"
  targetPort = 3000
  port = 8888
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[functions]
  directory = "netlify/functions"
